
  # SP-R1

  This is a code bundle for SP-R1. The original project is available at https://www.figma.com/design/u5yTHSyywXEOq75t0illyl/SP-R1.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  

